{
  "name": "cijing-3600",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:source": "node scripts/assemble.mjs",
    "dev": "npm run prepare:source && vite",
    "build": "npm run prepare:source && tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "npm run prepare:source && vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest"
  }
}
